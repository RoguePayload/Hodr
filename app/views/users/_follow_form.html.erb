<% unless current_user?(@user) %>
   <div id="follow_form" style="width: 35%; float: right;">
   <% if current_user.following?(@user) %>
     <%= render 'unfollow' %>
   <% else %>
     <%= render 'follow' %>
   <% end %>
   </div>
<% end %>
