<% mobile_view = browser.device.mobile? %>

<% if mobile_view %>
  <!-- Mobile-specific view -->
  <%= render 'home_mobile' %>
<% else %>
    <!-- Default view for desktop -->
    <%= render 'home_desktop' %>
<% end %>
